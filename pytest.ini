[pytest]
# pytest configuration file

# Python test files matching
python_files = test_*.py

# Test directories
testpaths = tests

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers

# Markers
markers =
    unit: Unit tests
    integration: Integration tests
    contract: Contract tests
    security: Security tests

